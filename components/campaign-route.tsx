"use client";
import { motion, useScroll, useSpring, useTransform } from "motion/react";
import { useRef } from "react";
export default function CampaignRoute(){const ref=useRef<HTMLElement>(null);const {scrollYProgress}=useScroll({target:ref,offset:["start end","end start"]});const progress=useSpring(scrollYProgress,{stiffness:90,damping:22});const y=useTransform(progress,[0,1],[0,330]);return <section ref={ref} className="route-section"><div className="container route-grid"><div className="route-intro"><div className="eyebrow">A trilha da solidariedade</div><h2>Você não precisa correr sozinho.</h2><p>Deslize pela jornada: cada parada transforma uma intenção em um gesto concreto.</p></div><div className="route-visual"><div className="route-road"><div className="route-lane"/><div className="route-lane lane-2"/><motion.div className="route-dot" style={{y}}/><span className="route-point point-a"><b>01</b><small>inscrição</small></span><span className="route-point point-b"><b>02</b><small>alimento</small></span><span className="route-point point-c"><b>03</b><small>confirmação</small></span></div></div></div></section>}
