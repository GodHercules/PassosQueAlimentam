import { test, expect } from '@playwright/test';
test('landing page presents the event and primary CTA', async ({page})=>{await page.goto('/');await expect(page.getByRole('heading',{name:/Todo passo pode alimentar/i})).toBeVisible();await expect(page.getByRole('link',{name:/Fazer minha pré-inscrição/i})).toHaveAttribute('href','/cadastro');});
test('registration password validation is understandable', async ({page})=>{await page.goto('/cadastro');await page.getByLabel('Nome completo').fill('Pessoa Teste');await page.getByLabel('E-mail').fill('teste@example.com');await page.getByLabel('Senha').fill('curta');await page.getByRole('checkbox').check();await page.getByRole('button',{name:'Criar minha conta'}).click();await expect(page.getByRole('alert')).toContainText('pelo menos 10');});
