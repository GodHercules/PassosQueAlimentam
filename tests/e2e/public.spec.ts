import { test, expect } from '@playwright/test';
test('landing page presents the event and primary CTA', async ({page})=>{await page.goto('/');await expect(page.getByRole('heading',{name:/Passos que alimentam futuros/i})).toBeVisible();await expect(page.getByRole('link',{name:/Fazer minha pré-inscrição/i})).toHaveAttribute('href','/pre-inscricao');});
test('registration password field enforces the minimum length', async ({page})=>{await page.goto('/cadastro');const password=page.locator('input[name="password"]');await expect(password).toHaveAttribute('minlength','10');await password.fill('curta');await expect(password).toHaveJSProperty('validity.valid',false);});
